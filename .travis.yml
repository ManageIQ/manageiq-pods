---
jobs:
- language: ruby
  rvm: 2.6.5
  cache:
    bundler: true
  before_script:
  - eval "$(gimme 1.14)"
  script:
  - bundle exec rspec
- language: ruby
  rvm: 2.5.7
  cache:
    bundler: true
  before_script:
  - eval "$(gimme 1.14)"
  script:
  - bundle exec rspec
  
#ppc64le jobs
- arch: ppc64le
  language: ruby
  rvm: 2.6.5
  cache:
    bundler: true
  before_script:
  - eval "$(gimme 1.14)"
  script:
  - bundle exec rspec
- arch: ppc64le
  language: ruby
  rvm: 2.5.7
  cache:
    bundler: true
  before_script:
  - eval "$(gimme 1.14)"
  script:
  - bundle exec rspec
