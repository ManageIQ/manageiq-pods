#!/bin/bash

if [ -n "$CI" ]; then
  echo "== Installing operator-sdk =="
  curl -L https://github.com/operator-framework/operator-sdk/releases/download/v1.34.1/operator-sdk_linux_amd64 -o /usr/local/bin/operator-sdk
  chmod +x /usr/local/bin/operator-sdk
fi
